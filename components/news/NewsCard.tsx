'use client'; import Link from 'next/link'; import Image from 'next/image'; import { NewsBase } from '@/types/news';
export default function NewsCard({ news }: { news: NewsBase }) { return ( <Link href={`/news/${news.slug.current}`} className="group block"> <article className="bg-g-secondary rounded-xl overflow-hidden h-full flex flex-col"> <div className="relative h-48"><Image src={news.featuredImage} alt={news.title} fill className="object-cover group-hover:scale-110 transition" /></div> <div className="p-5 flex-1 flex flex-col"><h3 className="text-lg font-bold text-white mb-2">{news.title}</h3><p className="text-gray-400 text-sm mb-4">{news.excerpt}</p></div> </article> </Link> ); }
