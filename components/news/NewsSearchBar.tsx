'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function NewsSearchBar() { const[q, setQ] = useState(''); const r = useRouter(); return ( <form onSubmit={(e) => { e.preventDefault(); if(q) r.push(`/news?q=${q}`); }}><input type="text" value={q} onChange={(e)=>setQ(e.target.value)} placeholder="Search news..." className="w-full px-6 py-4 rounded-full text-black" /></form> ); }
