import Link from 'next/link'; import Image from 'next/image'; import { NewsBase } from '@/types/news';
export default function TrendingNews({ news }: { news: NewsBase[] }) { return ( <div className="bg-g-secondary p-6 rounded-xl"><h3 className="text-xl font-black text-white mb-6">Trending Now</h3><div className="space-y-4">{news.slice(0,5).map((n, i) => ( <Link key={n._id} href={`/news/${n.slug.current}`} className="flex gap-3"><div className="w-8 h-8 bg-orange-500 rounded-full flex items-center justify-center text-white font-bold">{i+1}</div><h4 className="text-sm font-bold text-white">{n.title}</h4></Link> ))}</div></div> ); }
