'use client'; import { useState } from 'react'; import Link from 'next/link'; import { NewsBase } from '@/types/news';
export default function BreakingNewsBar({ news }: { news: NewsBase[] }) { const [v, setV] = useState(true); if(!v || !news.length) return null; return ( <div className="bg-red-600 text-white py-3"><div className="container mx-auto px-4"><Link href={`/news/${news[0].slug.current}`} className="font-medium">BREAKING: {news[0].title}</Link></div></div> ); }
